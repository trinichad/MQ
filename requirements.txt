flask>=3.0
flask-socketio>=5.3

# IR sensor on Raspberry Pi (skipped on macOS / Windows).
# gpiozero needs a pin-factory backend; lgpio works on Pi 4 + Pi 5
# with current kernels (RPi.GPIO is broken on newer kernels).
gpiozero>=2.0; platform_system == "Linux"
lgpio; platform_system == "Linux"
