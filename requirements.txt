flask>=3.0
flask-socketio>=5.3
# IR sensor (only installs/works on Raspberry Pi; safe to skip on desktop)
gpiozero>=2.0; platform_system == "Linux"
# RPi.GPIO is the default backend for gpiozero on the Pi
RPi.GPIO; platform_system == "Linux" and platform_machine != "x86_64"
